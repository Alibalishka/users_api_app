import 'package:flutter/material.dart';

class AppPaddings {
  static const EdgeInsets horizontal = EdgeInsets.symmetric(horizontal: 48);
  static const EdgeInsets primary = EdgeInsets.symmetric(horizontal: 15, vertical: 143);
}